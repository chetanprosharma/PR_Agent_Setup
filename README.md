# PR Agent + Ollama Setup

## Local Development
1. Install Docker + Ollama in WSL Ubuntu in your base URL server or in local environment.
2. Pull models:
   ```bash
      model="ollama/qwen2.5-coder:7b"
      fallback_model : "ollama/qwen2.5-coder:3b"
